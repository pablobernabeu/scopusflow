% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-analyse.R
\name{plot_scopus_trend}
\alias{plot_scopus_trend}
\alias{autoplot.scopus_trend}
\title{Plot a publication trend}
\usage{
plot_scopus_trend(x, ...)

\method{autoplot}{scopus_trend}(object, ...)
}
\arguments{
\item{x}{A \code{scopus_trend} object from \code{\link[=scopus_trend]{scopus_trend()}}.}

\item{...}{Currently unused, present for S3 consistency.}

\item{object}{A \code{scopus_trend} object (for the \code{autoplot()} method).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object. Needs the suggested package \pkg{ggplot2}.
}
\description{
Draws annual record counts over time from the output of \code{\link[=scopus_trend]{scopus_trend()}}.
}
\examples{
\dontshow{if (rlang::is_installed("ggplot2")) withAutoprint(\{ # examplesIf}
# Drawn from the bundled corpus of real articles, which needs no key. That
# corpus is a complete harvest, so its rows per year are the publications
# per year its query returns.
tr <- scopus_year_counts(example_records,
                         query = "TITLE-ABS-KEY(graphene supercapacitor)")
plot_scopus_trend(tr)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=scopus_trend]{scopus_trend()}}, \code{\link[=scopus_year_counts]{scopus_year_counts()}}
}
