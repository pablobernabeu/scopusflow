% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_plan.R
\name{scopus_fetch_plan}
\alias{scopus_fetch_plan}
\title{Execute a 'Scopus' search plan, with optional caching and resume}
\usage{
scopus_fetch_plan(
  plan,
  max_results = Inf,
  cache_dir = NULL,
  resume = TRUE,
  api_key = NULL,
  inst_token = NULL,
  verbose = FALSE
)
}
\arguments{
\item{plan}{A \code{scopus_plan} object from \code{\link[=scopus_plan]{scopus_plan()}}.}

\item{max_results}{Maximum records to retrieve per cell (default \code{Inf}).}

\item{cache_dir}{Optional directory for per-cell cache files. The default of
\code{NULL} performs no caching. Pass an explicit path you control, or
\code{scopus_cache_dir()} to use a managed, clearable cache under
\code{\link[tools:R_user_dir]{tools::R_user_dir()}}. Caching happens only when you opt in through this
argument. A cache directory serves one plan: cells are checkpointed by
their position in the plan and their year, so give each distinct plan its
own directory. As a safeguard, a checkpoint is served only when the query,
year, view and page size it was fetched under all match the plan cell, and
only when its own \code{max_results} did not truncate it below what is being
asked for now; anything else is a cache miss, refetched and overwritten.
A checkpoint holding more records than the current \code{max_results} asks for
is served trimmed to that cap, the fuller set staying on disk. A
checkpoint that cannot be read back, for example one left half-written by
an interrupted run, is also treated as a miss rather than aborting the
harvest.}

\item{resume}{Logical. When \code{TRUE} and \code{cache_dir} is set, a cell whose cache
file already exists is loaded from disk rather than fetched again.}

\item{api_key, inst_token}{Optional credentials (see \code{\link[=scopus_has_key]{scopus_has_key()}}).}

\item{verbose}{Logical. When \code{TRUE}, per-cell progress is reported.}
}
\value{
A \link{scopus_records} tibble combining all cells, with the originating
\code{plan} attached as the \code{plan} attribute and the per-cell accounting as
\code{cell_totals}, a tibble of \code{cell}, \code{date}, \code{n_records} and
\code{reported_total} (the count the API gave for that cell, \code{NA} where it gave
none). \code{total_results} is their sum, and is \code{NA} unless every cell
reported one, since a partial sum would understate the search while
looking like a real figure. A cell that comes back shorter than the API
said it should warns, because a truncated or refused download otherwise
arrives as a merely small result set; a cell stopped by \code{max_results} is
short by request and does not warn. \code{\link[=scopus_search_report]{scopus_search_report()}} reads all of
this back. The \code{retrieved_at} and
\code{scopusflow_version} attributes described in \code{\link[=scopus_fetch]{scopus_fetch()}} are carried
across from the cells: the time is the earliest of them, since a combined
set is only as fresh as its oldest cell, and every version that
contributed is listed, since resuming an older cache means more than one
did. Both are omitted when any cell cannot supply them, as a checkpoint
written before they existed cannot, rather than dating the whole from a
part of it.
}
\description{
Runs every cell of a \code{\link[=scopus_plan]{scopus_plan()}} in turn, optionally caching each cell's
result so that an interrupted or quota-limited retrieval can resume without
re-spending quota on the cells already fetched. Results are accumulated and
bound once into a single \link{scopus_records} tibble.
}
\section{API access}{

Any cell not served from cache requires a valid API key and internet access.
The \emph{API access} section of \code{\link[=scopus_count]{scopus_count()}} gives the details.
}

\examples{
\dontshow{if (scopusflow::scopus_has_key()) withAutoprint(\{ # examplesIf}
plan <- scopus_plan("graphene supercapacitor", years = 2015:2024,
                    field = "TITLE-ABS-KEY", partition = "year")
dir <- file.path(tempdir(), "graphene-cache")
# `max_results` caps each yearly cell, so the example stays small and
# quota-light; drop it to harvest every record in the plan.
recs <- scopus_fetch_plan(plan, max_results = 25, cache_dir = dir, resume = TRUE)
\dontshow{\}) # examplesIf}
# The offline companion, which needs no key: a record set with the plan
# that describes it attached. 'Scopus' records may not be redistributed, so
# the bundled corpus of real articles stands in for the harvest, and the
# plan describes the same search, one cell per year.
plan <- scopus_plan("graphene supercapacitor", years = 2015:2024,
                    field = "TITLE-ABS-KEY", partition = "year")
recs <- example_records
attr(recs, "plan") <- plan
recs
attr(recs, "plan")
}
\seealso{
\code{\link[=scopus_cache_dir]{scopus_cache_dir()}}, \code{\link[=scopus_cache_clear]{scopus_cache_clear()}}
}
