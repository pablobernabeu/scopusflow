% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{scopus_top}
\alias{scopus_top}
\title{Most frequent values in a record set}
\usage{
scopus_top(x, by = c("source", "author"), n = 10L)
}
\arguments{
\item{x}{A \link{scopus_records} tibble.}

\item{by}{What to tally: \code{"source"} (the publication titles) or \code{"author"}.
Author strings holding several names separated by \code{"; "} are split, so each
contributor is counted once per record.}

\item{n}{The number of rows to return (the top \code{n}).}
}
\value{
A tibble of class \code{scopus_top} with columns \code{value} and \code{n}, sorted by
descending count, with ties broken by \code{value} in byte order so the result is
reproducible across platforms and locales. Exactly \code{n} rows are returned
(fewer if there are fewer distinct values), so values tied at the cut-off
rank may be dropped. The \code{by} choice is stored in the \code{by} attribute.
}
\description{
Tallies the most common sources or authors across a \link{scopus_records} object.
It works on records already in memory, so it makes no network request.
}
\examples{
# The bundled corpus of real articles stands in for a harvest of your own,
# since 'Scopus' records may not be redistributed.
scopus_top(example_records, by = "source")

# That corpus names one author per article, so the author tally counts
# first authors; a live harvest lists every author and splits them.
scopus_top(example_records, by = "author", n = 5)
}
\seealso{
\code{\link[=plot_scopus_top]{plot_scopus_top()}}, \code{\link[=summary.scopus_records]{summary.scopus_records()}}
}
