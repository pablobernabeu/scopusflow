% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{scopus_year_counts}
\alias{scopus_year_counts}
\title{Tally the records of a harvest by year}
\usage{
scopus_year_counts(x, query = NA_character_)
}
\arguments{
\item{x}{A \link{scopus_records} object.}

\item{query}{Optional string naming the query the records answer, recorded
in the \code{query} column. Records do not carry the query they came from, so
it is \code{NA} unless supplied.}
}
\value{
A tibble of class \code{scopus_trend} with the columns \code{query}, \code{year}
(integer) and \code{n} (the count that year, as a double, matching
\code{\link[=scopus_trend]{scopus_trend()}}). Records with no year are dropped, and a year no record
falls in is absent rather than zero, since a harvest says nothing about a
year it does not cover.
}
\description{
Counts the records of a set already in hand by publication year, in the
shape \code{\link[=scopus_trend]{scopus_trend()}} returns. A complete harvest holds its own annual
counts, so this gives the trend of that query without spending a request.
}
\examples{
# The bundled corpus of real articles is a complete harvest of its own
# query, so its rows per year are the publications per year that query
# returns.
scopus_year_counts(example_records,
                   query = "TITLE-ABS-KEY(graphene supercapacitor)")
}
\seealso{
\code{\link[=scopus_trend]{scopus_trend()}} for the same table counted through the API, and
\code{\link[=plot_scopus_trend]{plot_scopus_trend()}} to draw either.
}
