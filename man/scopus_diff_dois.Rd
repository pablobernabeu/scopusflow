% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dois.R
\name{scopus_diff_dois}
\alias{scopus_diff_dois}
\title{Compare two DOI retrievals}
\usage{
scopus_diff_dois(old, new)
}
\arguments{
\item{old, new}{\link{scopus_records} objects or character vectors of DOIs,
representing the earlier (\code{old}) and later (\code{new}) retrievals.}
}
\value{
A tibble with columns \code{doi} and \code{status}, where \code{status} is one of
\code{"added"} (in \code{new} only), \code{"removed"} (in \code{old} only) or \code{"unchanged"} (in
both), sorted by status then DOI.
}
\description{
Identifies which DOIs were added, removed or unchanged between an earlier and
a later retrieval. This supports change tracking: re-running a search later
and seeing exactly what is new.
}
\examples{
old <- c("10.1/a", "10.1/b")
new <- c("10.1/b", "10.1/c")
scopus_diff_dois(old, new)
}
\seealso{
\code{\link[=scopus_extract_dois]{scopus_extract_dois()}}
}
