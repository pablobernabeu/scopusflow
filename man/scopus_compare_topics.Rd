% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{scopus_compare_topics}
\alias{scopus_compare_topics}
\title{Compare publication trends across topics}
\usage{
scopus_compare_topics(
  reference_query,
  comparison_terms,
  years,
  field = NULL,
  view = c("STANDARD", "COMPLETE"),
  api_key = NULL,
  inst_token = NULL,
  verbose = FALSE
)
}
\arguments{
\item{reference_query}{Character scalar. The reference topic that anchors the
comparison (for example \code{"language learning"}).}

\item{comparison_terms}{Character vector of topics to compare against the
reference (for example \code{c("effect size", "Bayesian")}). Each is combined
with the reference using a logical AND.}

\item{years}{Integer vector of publication years to span (for example
\code{2015:2020}).}

\item{field}{Optional 'Scopus' field tag applied to every component of every
query (see \code{\link[=scopus_plan]{scopus_plan()}}).}

\item{view}{Either \code{"STANDARD"} or \code{"COMPLETE"}.}

\item{api_key, inst_token}{Optional credentials (see \code{\link[=scopus_has_key]{scopus_has_key()}}).}

\item{verbose}{Logical. When \code{TRUE}, progress is reported.}
}
\value{
A tibble of class \code{scopus_comparison} with the columns \code{query} (the
full query used), \code{query_type} (\code{"reference"} or \code{"comparison"}),
\code{abridged_query} (the topic label for plotting), \code{year}, \code{n} (records that
year), \code{reference_n} (reference records that year), \code{comparison_percentage}
(\code{100 * n / reference_n}, or \code{NA} when \code{reference_n} is 0) and
\code{average_comparison_percentage} (the same ratio computed on period totals).
Comparison rows are sorted by descending average percentage.
}
\description{
Compares how often a set of \emph{comparison} topics co-occur with a \emph{reference}
topic over time. For each year and each comparison term, the number of records
matching the reference combined with that term is expressed as a percentage of
the records matching the reference alone. This reveals which sub-topics are
growing or shrinking within a literature.
}
\section{API access}{

This performs one count request per term per year, so it requires a valid API
key and internet access. The \emph{API access} section of \code{\link[=scopus_count]{scopus_count()}} gives
the details. A modest number of terms and years keeps the call within quota.
}

\examples{
\dontshow{if (scopusflow::scopus_has_key()) withAutoprint(\{ # examplesIf}
cmp <- scopus_compare_topics(
  reference_query = "deep learning",
  comparison_terms = c("computer vision", "drug discovery"),
  years = 2018:2022,
  field = "TITLE-ABS-KEY"
)
cmp
\dontshow{\}) # examplesIf}
# The shape of the return value, built offline so it runs without a key.
years <- 2018:2022
ref_n <- c(4200, 5600, 7100, 8600, 10200)
counts <- list(`computer vision` = c(1500, 2000, 2500, 3000, 3600),
               `drug discovery`  = c(180, 260, 370, 500, 660))
cmp <- tibble::tibble(
  query = "TITLE-ABS-KEY(deep learning)",
  query_type = c(rep("reference", length(years)),
                 rep("comparison", length(counts) * length(years))),
  abridged_query = c(rep("deep learning", length(years)),
                     rep(names(counts), each = length(years))),
  year = rep(years, length(counts) + 1),
  n = c(ref_n, unlist(counts, use.names = FALSE)),
  reference_n = rep(ref_n, length(counts) + 1),
  comparison_percentage = 100 * c(ref_n, unlist(counts, use.names = FALSE)) /
    rep(ref_n, length(counts) + 1),
  average_comparison_percentage = c(rep(100, length(years)),
                                    rep(c(35.3, 5.4), each = length(years)))
)
class(cmp) <- c("scopus_comparison", class(cmp))
cmp
}
\seealso{
\code{\link[=plot_scopus_comparison]{plot_scopus_comparison()}} to visualise the result.
}
