% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{scopus_plan}
\alias{scopus_plan}
\alias{is_scopus_plan}
\title{Build a reproducible 'Scopus' search plan}
\usage{
scopus_plan(
  query,
  years = NULL,
  field = NULL,
  view = c("STANDARD", "COMPLETE"),
  page_size = NULL,
  partition = c("none", "year")
)

is_scopus_plan(x)
}
\arguments{
\item{query}{Character scalar. The base search expression, without field tags
or year filters (these are added through \code{field} and \code{years}).}

\item{years}{Optional integer vector of publication years to restrict to, for
example \code{2015:2020}. When \code{partition = "year"}, one plan cell is created per
distinct year; otherwise the minimum and maximum define a single date range.}

\item{field}{Optional character scalar naming a 'Scopus' field tag to wrap the
query in, for example \code{"TITLE-ABS-KEY"}, \code{"TITLE"}, \code{"AUTH"} or \code{"AFFIL"}.
When \code{NULL}, the query is used verbatim.}

\item{view}{Either \code{"STANDARD"} or \code{"COMPLETE"}. \code{COMPLETE} returns more
fields but requires a subscriber entitlement and is limited to a smaller
page size.}

\item{page_size}{Integer number of records to request per page, or \code{NULL}
(the default) to use the largest page the view allows. The 'Scopus' Search
API permits up to 200 records per page for the \code{STANDARD} view but only 25
for \code{COMPLETE}. Because the weekly quota is charged per request, requesting
the maximum page size minimises the number of requests - and hence the
quota - needed to retrieve a given result set. Lower it only if you have a
reason to.}

\item{partition}{Either \code{"none"} (a single query cell) or \code{"year"} (one cell
per year in \code{years}). Partitioning by year is the recommended way to stay
under the API's hard limit of \code{start < 5000}.}

\item{x}{An object to test or print.}
}
\value{
A tibble of class \code{scopus_plan}, one row per cell, with columns
\code{cell}, \code{query} (field-wrapped), \code{date} (year range string or \code{NA}), \code{year}
(integer or \code{NA}), \code{view} and \code{page_size}. Plan-level settings are stored as
attributes.

\code{is_scopus_plan()} returns a length-one logical.
}
\description{
A \emph{plan} is a fully specified, inspectable description of one or more 'Scopus'
queries to run. Splitting the act of \emph{describing} a search from \emph{executing} it
makes workflows reproducible (the plan can be saved, reviewed and version
controlled) and lets large retrievals be partitioned, for example one cell per
year, so they can be cached and resumed.
}
\examples{
scopus_plan("language learning", years = 2018:2020, field = "TITLE-ABS-KEY")
scopus_plan("machine translation", years = 2010:2020, partition = "year")
}
\seealso{
\code{\link[=scopus_fetch_plan]{scopus_fetch_plan()}} to execute a plan, \code{\link[=scopus_count]{scopus_count()}} to size it.
}
