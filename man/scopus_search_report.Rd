% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{scopus_search_report}
\alias{scopus_search_report}
\alias{format.scopus_search_report}
\alias{print.scopus_search_report}
\title{Assemble a reproducible record of a 'Scopus' search}
\usage{
scopus_search_report(x, plan = NULL, file = NULL)

\method{format}{scopus_search_report}(x, style = c("report", "paragraph", "markdown"), ...)

\method{print}{scopus_search_report}(x, ...)
}
\arguments{
\item{x}{A \link{scopus_records} object, which supplies the counts and the
retrieval provenance and, through its \code{plan} attribute, the plan; or a bare
\code{\link[=scopus_plan]{scopus_plan()}} for a search that has not been run yet. For \code{format()} and
\code{print()}, the report object itself.}

\item{plan}{Optional \code{\link[=scopus_plan]{scopus_plan()}} describing \code{x}. Supply it when a record
set does not carry one, for example one retrieved with \code{\link[=scopus_fetch]{scopus_fetch()}} or
read back from a \code{.csv}. An explicit plan takes precedence over the one \code{x}
carries.}

\item{file}{Optional path at which to write the record as Markdown. A file is
written only when this argument is supplied, and only to the exact path
given, so the package leaves the working directory untouched unless asked.
Parent directories are assumed to exist already.}

\item{style}{Which rendering to return: \code{"report"} for the readable record
that \code{print()} shows, \code{"paragraph"} for the methods paragraph, or
\code{"markdown"} for the whole record as Markdown, which is what \code{file} writes.}

\item{...}{Ignored, present for compatibility with the generics.}
}
\value{
A list of class \code{scopus_search_report}, returned invisibly when
\code{file} is written. Its elements are the fields the record is built from:
\code{database}, \code{platform}, \code{query} (the base query), \code{field}, \code{expression}
(the field-wrapped query of each cell), \code{view}, \code{page_size}, \code{paging},
\code{partition}, \code{n_cells}, \code{years}, \code{cells} (a tibble of \code{cell}, \code{limit},
\code{n_records} and \code{reported_total}), \code{searched_at}, \code{version}, \code{n_records},
\code{n_with_doi}, \code{reported_total}, \code{cells_reported} (how many cells reported a
total), \code{records_combined}, \code{duplicates_removed},
\code{deduplicated}, \code{snippet} and \code{prisma} (a tibble of \code{item}, \code{name},
\code{source} and \code{note}). A field the objects do not record is \code{NA}.

\code{format()} returns a length-one character string.
}
\description{
Turns a harvest, or a plan not yet run, into the search-strategy record a
systematic review has to report: what was searched, exactly how, when, how
much came back, and how much the API said there was. The record prints as a
readable report, formats as a methods paragraph fit to paste into a
manuscript, and writes as Markdown. The reporting standard it follows is
PRISMA-S (Rethlefsen et al., 2021), together with the identification counts
of the PRISMA 2020 flow diagram.
}
\details{
Everything in the record comes from the objects handed to it. The date of the
search is the \code{retrieved_at} attribute \code{\link[=scopus_fetch]{scopus_fetch()}} attaches, never the
current time; the number of records the API reported as matching is what the
objects recorded (the \code{cell_totals} attribute \code{\link[=scopus_fetch_plan]{scopus_fetch_plan()}} attaches,
or, where no cell counts travelled with the set, its own \code{total_results}),
never an inference from the number of rows, and where the cells carry counts
it is given overall only when every cell reported one; and the duplicates
removed are those \code{\link[=scopus_combine]{scopus_combine()}} recorded removing. Where
an attribute is absent, as it is for a set read back from a \code{.csv}, for the
bundled corpus, and for a cell resumed from a checkpoint written before these
attributes existed, the record says the field is unrecorded and fills
nothing in. This matters most for completeness: a harvest whose reported
total is unknown is never described as exhaustive.

The PRISMA-S map is decided the same way. Items the package holds evidence
for (the database and platform, the full strategy, the limits, the date, the
totals, and de-duplication where it was performed) are listed as supplied.
The rest, among them peer review of the strategy, grey literature, other
databases and citation searching, are listed as the author's to supply,
because the package has no way to know them.
}
\examples{
# A search described but not yet run. The record says so throughout rather
# than implying figures it cannot have.
plan <- scopus_plan("graphene supercapacitor", years = 2015:2024,
                    field = "TITLE-ABS-KEY", partition = "year")
scopus_search_report(plan)

# The same search after a harvest. The bundled corpus of real articles
# stands in for one, since 'Scopus' records may not be redistributed, so the
# attributes a live retrieval records are set here by hand.
recs <- example_records
attr(recs, "plan") <- plan
attr(recs, "retrieved_at") <- as.POSIXct("2026-07-22 09:15:00", tz = "UTC")
attr(recs, "scopusflow_version") <- "0.3.0"
report <- scopus_search_report(recs)
report

# The methods paragraph, and the Markdown record for a supplementary file.
cat(format(report, style = "paragraph"))
path <- tempfile(fileext = ".md")
scopus_search_report(recs, file = path)
}
\references{
Rethlefsen, M. L., Kirtley, S., Waffenschmidt, S., Ayala, A. P., Moher, D.,
Page, M. J., & Koffel, J. B. (2021). PRISMA-S: an extension to the PRISMA
Statement for Reporting Literature Searches in Systematic Reviews.
\emph{Systematic Reviews}, \emph{10}, 39. \doi{10.1186/s13643-020-01542-z}
}
\seealso{
\code{\link[=scopus_plan]{scopus_plan()}}, \code{\link[=scopus_fetch_plan]{scopus_fetch_plan()}}, \code{\link[=scopus_combine]{scopus_combine()}}
}
