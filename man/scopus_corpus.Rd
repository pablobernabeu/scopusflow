% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{scopus_corpus}
\alias{scopus_corpus}
\title{Assemble a minimal, cross-tool corpus with keywords and references}
\usage{
scopus_corpus(
  records,
  by = c("doi", "scopus_id"),
  view = c("FULL", "REF"),
  cache_dir = NULL,
  resume = TRUE,
  api_key = NULL,
  inst_token = NULL,
  verbose = FALSE
)
}
\arguments{
\item{records}{A \code{\link[=scopus_records]{scopus_records()}} tibble, or any data frame with \code{doi},
\code{title} and \code{year} columns in the same shape.}

\item{by}{Either \code{"doi"} or \code{"scopus_id"}, the kind of identifier in
\code{records} to look records up by (see \code{\link[=scopus_abstract]{scopus_abstract()}}).}

\item{view}{Either \code{"FULL"} (the default) or \code{"REF"}, passed to
\code{\link[=scopus_abstract]{scopus_abstract()}}. \code{"FULL"} is recommended: in development, it
returned a complete, correctly counted reference list for every document
tried, while \code{"REF"} returned an inconsistent, sometimes-truncated
subset (see \code{\link[=scopus_abstract]{scopus_abstract()}}'s documentation for the details and for
the entitlement each view needs).}

\item{cache_dir, resume}{As in \code{\link[=scopus_abstract]{scopus_abstract()}}: an optional directory
for per-identifier cache files, and whether an existing one is reused.
Worth setting for anything beyond a handful of records, since this
performs one Abstract Retrieval request per record, against its own,
smaller weekly quota.}

\item{api_key, inst_token}{Optional credentials (see \code{\link[=scopus_has_key]{scopus_has_key()}}).}

\item{verbose}{Logical. When \code{TRUE}, progress is reported.}
}
\value{
A tibble with columns \code{id} (the identifier \code{records} was looked up
by), \code{title}, \code{year}, \code{keywords} (a list-column: a character vector of
the document's author keywords, split out of \code{\link[=scopus_abstract]{scopus_abstract()}}'s
joined \code{authkeywords} string, empty when the document has none or the
field is unavailable) and \code{references} (a list-column: each entry is the
\code{references} data frame \code{\link[=scopus_abstract]{scopus_abstract()}} returns for that document,
with one row per cited work). A record in \code{records} whose identifier is
\code{NA} is dropped, with a warning naming how many.
}
\description{
Takes a \code{\link[=scopus_records]{scopus_records()}} tibble, such as the output of \code{\link[=scopus_fetch]{scopus_fetch()}} or
\code{\link[=scopus_fetch_plan]{scopus_fetch_plan()}}, and enriches it with author keywords and structured
references via Abstract Retrieval, returning a minimal, uniform shape close
to what OpenAlex's \code{works} API already returns: an \code{id}, \code{title}, \code{year},
\code{keywords} (a list-column of character vectors) and \code{references} (a
list-column of data frames). This is meant for downstream tools that want to
consume 'Scopus' output without writing their own parsing layer, for example
for keyword co-occurrence or citation-network analysis. It does not replace
\code{\link[=as_bibliometrix]{as_bibliometrix()}}, which keeps its own established field-mapping
convention for users who want bibliometrix's tag names instead.
}
\section{API access}{

This performs one Abstract Retrieval request per usable record, on top of
whatever retrieved \code{records} in the first place; see \code{\link[=scopus_abstract]{scopus_abstract()}}'s
\emph{API access} section for the entitlement \code{view = "FULL"}/\code{"REF"} needs and
how a 403 is handled.
}

\examples{
\dontshow{if (scopusflow::scopus_has_key()) withAutoprint(\{ # examplesIf}
# Costs one Abstract Retrieval request per record, against a smaller,
# separate weekly quota from Search; see the API access section above.
recs <- scopus_fetch("DOI(10.1038/nature14539)", max_results = 1)
corpus <- scopus_corpus(recs)
corpus$keywords[[1]]
corpus$references[[1]]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=scopus_abstract]{scopus_abstract()}}, \code{\link[=as_bibliometrix]{as_bibliometrix()}}
}
