% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{write_scopus_records}
\alias{write_scopus_records}
\alias{read_scopus_records}
\title{Read and write 'Scopus' record sets}
\usage{
write_scopus_records(x, path)

read_scopus_records(path)
}
\arguments{
\item{x}{A \link{scopus_records} tibble to write.}

\item{path}{Explicit file path. The functions read from, or write to, exactly
this path and leave the working directory alone. Parent directories are
assumed to exist already.}
}
\value{
\code{write_scopus_records()} returns \code{x} invisibly. \code{read_scopus_records()}
returns a \link{scopus_records} tibble.
}
\description{
Save a \link{scopus_records} tibble to disk and read it back, with a stable
round-trip. The file extension selects the format. An \code{.rds} file preserves
the types and class exactly, while a \code{.csv} file is portable plain text.
The optional \code{authkeywords} column a \code{view = "COMPLETE"} retrieval adds
(see \code{\link[=scopus_records]{scopus_records()}}) round-trips through both formats. The attributes a
live retrieval carries, including \code{retrieved_at} and \code{scopusflow_version}
(see \code{\link[=scopus_fetch]{scopus_fetch()}}), survive the \code{.rds} form only: \code{.csv} is a table of
columns and cannot hold them, so save as \code{.rds} when a set is a baseline to
be compared against later.
}
\details{
A \code{.csv} written by another tool reads back too. It is taken as UTF-8
whatever the session's locale, and a blank field is read as missing, which is
how pandas, 'Excel' and the Python twin write one. A file carrying none of
\code{doi}, \code{title} and \code{year} is refused rather than read as a set of empty
records, and one carrying only part of the schema warns.
}
\examples{
# A round trip on the bundled corpus of real articles, which stands in for
# a retrieval of your own because 'Scopus' records may not be redistributed.
# The .rds form restores the object exactly.
rds <- tempfile(fileext = ".rds")
write_scopus_records(example_records, rds)
identical(read_scopus_records(rds), example_records)

# The .csv form is portable plain text and reads back to the same schema.
csv <- tempfile(fileext = ".csv")
write_scopus_records(example_records, csv)
head(read_scopus_records(csv))
}
