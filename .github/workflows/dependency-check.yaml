# Scheduled "dependency canary": builds and checks the package against the
# current and development versions of every Import, and of ggplot2, the one
# Suggest with a real breakage surface here (it backs all three plot files and
# gates every plotting example). A breaking change upstream, for example in
# httr2, is then caught early. The remaining Suggests are reported in the
# version dump below but their development heads are not installed. This
# workflow only observes
# and reports. It installs and executes the development heads of the
# dependencies, which is untrusted code, so it runs with a read-only checkout
# plus issue access and never holds a token that could push commits or open
# pull requests. On failure it flags the maintainer with a de-duplicated issue,
# including a free GitHub Models diagnosis when no Claude credential is
# configured, and then finishes red. That red conclusion is what triggers the
# companion dependency-autofix.yaml workflow, which holds the write permissions
# and attempts the agentic fix in a separate run.
on:
  schedule:
    - cron: "17 5 */2 * *" # every other day at 05:17 UTC
  workflow_dispatch:

name: dependency-check.yaml

permissions:
  contents: read
  issues: write
  models: read # free GitHub Models inference for the no-cost diagnosis path

jobs:
  dependency-check:
    runs-on: ubuntu-latest
    env:
      # The token behind GITHUB_PAT carries only the read-and-issues grants
      # declared above. pak needs it to install the development heads from
      # GitHub without tripping the anonymous API rate limit.
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      # Evaluates to the string "true" only when a Claude credential is
      # configured; the secret values themselves are never exposed. A Claude
      # subscription OAuth token (CLAUDE_CODE_OAUTH_TOKEN) drives the agentic
      # auto-fix at no API cost; an ANTHROPIC_API_KEY is the paid alternative.
      # When true, the free GitHub Models diagnosis below is skipped, because
      # the companion dependency-autofix.yaml workflow will attempt a full fix.
      HAS_CLAUDE: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN != '' || secrets.ANTHROPIC_API_KEY != '' }}

    steps:
      - uses: actions/checkout@v7

      - uses: r-lib/actions/setup-pandoc@d3c5be51b12e724e68f33216ca3c148b66d5f0b6  # v2.12.1

      - uses: r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6  # v2.12.1
        with:
          r-version: release
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@d3c5be51b12e724e68f33216ca3c148b66d5f0b6  # v2.12.1
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - name: Install development versions of the key dependencies
        # Tolerated rather than fatal, because a GitHub outage or a compile
        # error in somebody else's development head should not turn a two-day
        # cron red. The step below is what stops that tolerance from being
        # silent.
        continue-on-error: true
        shell: Rscript {0}
        run: |
          pak::pak(c(
            "r-lib/httr2", "r-lib/cli", "r-lib/rlang",
            "tidyverse/tibble", "jeroen/jsonlite", "tidyverse/ggplot2"
          ), ask = FALSE)

      - name: Record installed dependency versions
        shell: Rscript {0}
        run: |
          # Because the install above is continue-on-error, R CMD check would
          # otherwise run against the plain CRAN releases and pass, and the
          # canary would finish green having exercised nothing it exists to
          # exercise. Comparing each installed version against the release it
          # would otherwise have had says plainly whether any development head
          # landed. A warning and a step-summary line rather than a failure,
          # matching the dormancy notice in live-api-check.yaml: red on every
          # transient pak outage would be noise, but silence would be a lie.
          dev_heads <- c("httr2", "cli", "rlang", "tibble", "jsonlite", "ggplot2")
          reported_only <- c("shiny", "bslib", "callr")
          released <- tryCatch(available.packages()[, "Version"],
                               error = function(e) character())
          ahead <- character()
          for (p in c(dev_heads, reported_only)) {
            have <- tryCatch(as.character(packageVersion(p)),
                             error = function(e) NA_character_)
            rel <- if (p %in% names(released)) released[[p]] else NA_character_
            is_dev <- !is.na(have) && !is.na(rel) &&
              package_version(have) > package_version(rel)
            if (is_dev && p %in% dev_heads) ahead <- c(ahead, p)
            cat(sprintf(
              "%-10s %-16s release %-16s %s\n", p,
              if (is.na(have)) "absent" else have,
              if (is.na(rel)) "unknown" else rel,
              if (is_dev) "<- development head" else ""
            ))
          }
          note <- if (length(ahead)) {
            sprintf("Development heads in use: %s.", paste(ahead, collapse = ", "))
          } else if (!length(released)) {
            paste("The release versions could not be read, so whether any",
                  "development head landed is unknown for this run.")
          } else {
            paste("No development head is ahead of its CRAN release, so this run",
                  "checked the package against the ordinary releases only. The",
                  "install step above most likely failed; see its log.")
          }
          if (!length(ahead)) cat(sprintf("::warning::%s\n", note))
          summary_file <- Sys.getenv("GITHUB_STEP_SUMMARY")
          if (nzchar(summary_file)) {
            cat(sprintf("### Dependency canary\n\n%s\n", note),
                file = summary_file, append = TRUE)
          }

      - name: Check the package
        id: check
        shell: Rscript {0}
        run: |
          res <- rcmdcheck::rcmdcheck(
            args = c("--no-manual", "--as-cran"),
            error_on = "never", quiet = FALSE
          )
          problems <- c(res$errors, res$warnings)
          writeLines(if (length(problems)) problems else "none", "check-problems.txt")
          status <- if (length(res$errors) || length(res$warnings)) "fail" else "pass"
          cat(sprintf("status=%s\n", status),
              file = Sys.getenv("GITHUB_OUTPUT"), append = TRUE)

      # Free path (no key, runs for everyone): GitHub Models diagnoses the
      # breakage and proposes a fix, which is included in the issue below.
      - name: Build the diagnosis prompt
        if: steps.check.outputs.status == 'fail' && env.HAS_CLAUDE != 'true'
        run: |
          {
            echo "The R package 'scopusflow' is failing its dependency check, most"
            echo "likely because an updated dependency changed its API. From the R"
            echo "CMD check output, the DESCRIPTION and the source-file list below,"
            echo "give a short diagnosis of the most probable cause, name the file"
            echo "and function that most likely needs changing, and propose a"
            echo "concrete minimal fix as a code snippet or unified diff. Be concise."
            echo
            echo "=== R CMD check problems ==="; cat check-problems.txt
            echo; echo "=== DESCRIPTION ==="; cat DESCRIPTION
            echo; echo "=== R/ source files ==="; ls R
          } > ai-prompt.txt

      - name: Diagnose with GitHub Models (free)
        id: diagnose
        if: steps.check.outputs.status == 'fail' && env.HAS_CLAUDE != 'true'
        continue-on-error: true
        uses: actions/ai-inference@v2
        with:
          prompt-file: ai-prompt.txt
          system-prompt: "You are an expert R package maintainer who fixes breakage caused by dependency updates."
          max-completion-tokens: 1200

      - name: Save the diagnosis
        if: steps.check.outputs.status == 'fail' && env.HAS_CLAUDE != 'true'
        continue-on-error: true
        env:
          # Passed via env (not interpolated into the script) so untrusted model
          # output cannot inject shell commands.
          RESPONSE: ${{ steps.diagnose.outputs.response }}
        run: |
          if [ -n "$RESPONSE" ]; then
            {
              echo "### Suggested fix (free, via GitHub Models)"
              echo
              printf '%s\n' "$RESPONSE"
            } > ai-suggestion.md
          fi

      - name: Flag the maintainer
        if: steps.check.outputs.status == 'fail'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          title="Scheduled dependency check is failing"
          {
            echo "The scheduled dependency check found problems when building and"
            echo "checking scopusflow against current and development versions of"
            echo "its dependencies."
            echo ""
            echo "Run log: $RUN_URL"
            echo ""
            echo "Problems reported by R CMD check:"
            echo '```'
            cat check-problems.txt
            echo '```'
            if [ "$HAS_CLAUDE" = "true" ]; then
              echo ""
              echo "The companion dependency-autofix workflow will attempt a full"
              echo "automatic fix once this run completes. Look for an open pull"
              echo "request from a branch named fix/dependency-canary-*."
            fi
            if [ -f ai-suggestion.md ]; then
              echo ""
              cat ai-suggestion.md
            fi
          } > issue-body.md
          gh label create dependencies --color ededed \
            --description "Dependency maintenance" 2>/dev/null || true
          existing=$(gh issue list --state open --search "$title in:title" \
            --json number --jq '.[0].number')
          if [ -n "$existing" ]; then
            gh issue comment "$existing" --body-file issue-body.md
          else
            gh issue create --title "$title" --body-file issue-body.md \
              --label dependencies
          fi

      - name: Resolve the flag when the check passes again
        if: steps.check.outputs.status == 'pass'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          title="Scheduled dependency check is failing"
          existing=$(gh issue list --state open --search "$title in:title" \
            --json number --jq '.[0].number')
          if [ -n "$existing" ]; then
            gh issue comment "$existing" \
              --body "The scheduled dependency check is passing again. Closing."
            gh issue close "$existing"
          fi

      - name: Fail the run when the check failed
        # None of the steps above fails the job itself (the check step records
        # its verdict instead of erroring, so the issue steps always get to
        # run), so without this the workflow would finish green even when R CMD
        # check found problems. Finishing red keeps the Actions overview honest
        # and is the signal the companion dependency-autofix.yaml workflow
        # listens for through its workflow_run trigger.
        if: steps.check.outputs.status == 'fail'
        run: exit 1
